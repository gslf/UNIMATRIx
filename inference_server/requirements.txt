# Pinned to a venv inside this folder. See README.md for install steps.
#
# NOTE: llama-cpp-python is intentionally NOT pinned here. Install the wheel
# matching your GPU/CUDA setup separately (see README §1b) — typically:
#   pip install <prebuilt-wheel-url-or-pip-index>
# This file only carries the *other* runtime dependencies. Listing the server
# extras explicitly (instead of using `llama-cpp-python[server]`) means they
# install correctly even when the wheel was installed directly by URL,
# which bypasses pip's extras resolution.
fastapi>=0.110
uvicorn[standard]>=0.27
sse-starlette>=1.6
pydantic-settings>=2.0
starlette>=0.40
huggingface_hub>=0.24
rich>=13.7
